name: Build APK Calculadora

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-20.04  # CAMBIADO a 20.04 (más estable)
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.8'  # CAMBIADO a 3.8 (más compatible)
    
    - name: Install system dependencies
      run: |
        sudo apt update
        sudo apt install -y \
          python3-pip \
          build-essential \
          git \
          python3-dev \
          ffmpeg \
          libsdl2-dev \
          libsdl2-image-dev \
          libsdl2-mixer-dev \
          libsdl2-ttf-dev \
          libportmidi-dev \
          libswscale-dev \
          libavformat-dev \
          libavcodec-dev \
          zlib1g-dev \
          libgstreamer1.0-0 \
          gstreamer1.0-plugins-base \
          gstreamer1.0-plugins-good \
          libffi-dev \
          autoconf \
          automake \
          libtool  # AÑADIDO
    
    - name: Install Python packages
      run: |
        pip install --upgrade pip
        pip install buildozer cython==0.29.19
    
    - name: Debug - List files
      run: |
        echo "Contenido del directorio:"
        ls -la
    
    - name: Configure and build APK
      run: |
        # Renombrar archivo si es necesario
        if [ -f "calc_kivy_precalculo.py" ]; then
          mv calc_kivy_precalculo.py main.py
        fi
        
        # Inicializar buildozer
        buildozer init
        
        # Configurar buildozer.spec - VERSIÓN SIMPLIFICADA
        sed -i 's/title = My Application/title = Calculadora Math/g' buildozer.spec
        sed -i 's/package.name = myapp/package.name = calculadoramath/g' buildozer.spec
        sed -i 's/package.domain = org.test/package.domain = com.math.app/g' buildozer.spec
        sed -i 's/requirements = python3,kivy/requirements = python3,kivy/g' buildozer.spec  # QUITADO pillow
        sed -i 's/# android.accept_sdk_license = False/android.accept_sdk_license = True/g' buildozer.spec
        
        # Compilar
        buildozer -v android debug
    
    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: Calculadora-APK
        path: ./bin/*.apk
      if: success()  # Solo si tuvo éxito
